import 'package:flutter/material.dart';

class ShareRouteBottomSheet extends StatelessWidget {
  const ShareRouteBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(20),
      child: Text("Route sharing feature coming soon...", style: TextStyle(fontSize: 16)),
    );
  }
}
